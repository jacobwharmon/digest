import os

def prepare_pdf(quote_clean, headlines_clean, series_clean):
    """[summary]

    Args:
        quote_clean ([type]): [description]
        headlines_clean ([type]): [description]
        series_clean ([type]): [description]

    Returns:
        [type]: [description]
    """
    pdf_clean = NULL
    return pdf_clean
